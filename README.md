# Plan

High-level planning that maps directly to human intention

## Structure

```
Shards/(Dev) Plan/
  shard.yaml              # Manifest
  init-plan.md    # Init file — shard context
  skills/                 # sk-plan-{name}.md
  workflows/              # wkfl-plan-{name}.md
  templates/              # tmp-plan-{name}.md
  knowledge/              # knw-plan-{name}.md
  assets/                 # ast-plan-{name}.{ext}
  install/                # Files to install into Mesh/
```
